#include <unistd.h>
#include "main.h"
/**
 * _islower - Check for lowercase character
 *
 * Return: Always 0 (Success)
 */
int _islower(int c);
{
	int c;

	for (c = 48; c <= 122; c++)
	{
		_putchar(c);
	}

	if (c = [a - z])
	{
		return (1);
		else
		return (0);
	}

}
